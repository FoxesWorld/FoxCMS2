diff --git a/engine/classes/modules/AdminPanel/AdminOptions.class.php b/engine/classes/modules/AdminPanel/AdminOptions.class.php
--- a/engine/classes/modules/AdminPanel/AdminOptions.class.php
+++ b/engine/classes/modules/AdminPanel/AdminOptions.class.php
@@ -612,9 +612,11 @@ final class AdminOptions {
                 'matchedArchives' => 0,
                 'ignoredArchives' => 0,
                 'ignoredCandidates' => [],
-                'mode' => 'file-names-only',
-                'versionSource' => 'archive-file-name',
-                'systemSource' => 'relative-path-or-file-name',
+                'requiredPlatforms' => ['windows-x86_64', 'linux-x86_64', 'macos-x86_64'],
+                'supportedPlatforms' => [],
+                'mode' => 'exact-runtime-profiles',
+                'versionSource' => 'archive-release-metadata-or-file-name',
+                'systemSource' => 'catalog-branch-and-release-metadata',
                 'error' => $error->getMessage(),
             ];
@@ -712,31 +714,29 @@ final class AdminOptions {
             }
             if ($field === 'jreVersion') {
                 $value = trim((string)$value);
                 if ($value !== '') {
-                    if (preg_match('/^(?:1\.)?([0-9]+)(?:\.[0-9]+)*$/D', $value, $versionMatch) !== 1
-                        || (int)$versionMatch[1] < 1
-                    ) {
+                    if (!RuntimeJdkCatalog::isValidSelectorSyntax($value)) {
                         $this->respond([
-                            'message' => 'Java runtime должен быть числовой версией JDK, например 17 или 21.',
+                            'message' => 'Некорректный Java runtime profile. Выберите runtime из каталога.',
                             'type' => 'error',
                         ], 400);
                     }
-                    $value = (string)(int)$versionMatch[1];
                     try {
-                        $normalizedVersion = $this->runtimeJdkCatalog->normalizeVersion($value);
-                        if ($normalizedVersion !== null) {
-                            $value = $normalizedVersion;
+                        $normalizedProfile = $this->runtimeJdkCatalog->normalizeProfile($value);
+                        if ($normalizedProfile !== null) {
+                            $value = $normalizedProfile;
                         } else {
-                            $runtimeWarning = 'JDK ' . $value
-                                . ' сохранён, но архивы этого семейства не найдены для Windows, Linux и macOS. '
-                                . 'Запуск сервера может быть недоступен до загрузки runtime.';
+                            $runtimeWarning = 'Java runtime profile «' . $value
+                                . '» сохранён как legacy-значение, но точные архивы для платформ не найдены. '
+                                . 'Выберите профиль из актуального каталога перед запуском.';
                         }
                     } catch (Throwable $error) {
-                        $runtimeWarning = 'JDK ' . $value
-                            . ' сохранён без проверки каталога runtime: ' . $error->getMessage();
+                        $runtimeWarning = 'Java runtime profile «' . $value
+                            . '» сохранён без проверки каталога: ' . $error->getMessage();
                     }
                 }
             }
@@ -775,7 +775,7 @@ final class AdminOptions {
         if ($enabled && (!isset($data['jreVersion']) || trim((string)$data['jreVersion']) === '')) {
             $this->respond([
-                'message' => 'Для включённого сервера Java runtime обязателен. Укажите major-версию JDK.',
+                'message' => 'Для включённого сервера Java runtime обязателен. Выберите полный runtime profile.',
                 'type' => 'error',
             ], 400);
         }
