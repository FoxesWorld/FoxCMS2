const form=document.querySelector('[data-maintenance-form]');const feedback=document.querySelector('[data-maintenance-feedback]');if(form instanceof HTMLFormElement&&feedback instanceof HTMLElement){form.addEventListener('submit',async event=>{event.preventDefault();const button=form.querySelector('button[type="submit"]');if(button instanceof HTMLButtonElement)button.disabled=true;feedback.classList.remove('is-success');feedback.textContent='Проверяем доступ…';try{const response=await fetch('/',{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/x-www-form-urlencoded;charset=UTF-8','Accept':'application/json'},body:new URLSearchParams(new FormData(form))});const text=await response.text();let payload={};try{payload=JSON.parse(text)}catch{}const message=typeof payload.message==='string'?payload.message:`Ошибка сервера: ${response.status}`;feedback.textContent=message;if(response.ok&&payload.type==='success'){feedback.classList.add('is-success');window.setTimeout(()=>window.location.reload(),450)}}catch{feedback.textContent='Не удалось связаться с сервером.'}finally{if(button instanceof HTMLButtonElement)button.disabled=false}})}
